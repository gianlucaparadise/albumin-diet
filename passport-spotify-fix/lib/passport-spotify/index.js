/**
 * Module dependencies.
 */
var Strategy = require('./strategy');

/**
 * Framework version.
 */
// require('pkginfo')(module, 'version'); // this line breaks ncc (webpack)

/**
 * Expose constructors.
 */
exports.Strategy = Strategy;
